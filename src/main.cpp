/// @file main.cpp
/// @brief Parallax entry point — scaffold placeholder (Task 1.1)

#include <spdlog/spdlog.h>

#include <cstdlib>

int main()
{
    spdlog::info("Parallax v0.1.0 — scaffold build successful.");
    return EXIT_SUCCESS;
}